apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rest-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rest-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rest-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
